import React from 'react';
import Aside from '../aside';
import CarsContent from '../cars-content';

const Main = () => (
    <main className="main">
        <Aside />
        <CarsContent />
    </main>
);

export default Main;
